{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://spatialdsl-studio/schemas/omniverse-asset-manifest.schema.json",
  "title": "Omniverse Asset Manifest",
  "description": "Versioned manifest mapping DSL classes and elements to referenced Omniverse USD assets.",
  "type": "object",
  "required": ["version", "defaults"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Manifest schema version."
    },
    "defaults": {
      "type": "object",
      "description": "Per-class asset mapping, keyed by DSL class name.",
      "additionalProperties": {
        "$ref": "#/definitions/assetMapping"
      }
    },
    "overrides": {
      "type": "object",
      "description": "Per-element asset mapping, keyed by element name. Wins over defaults.",
      "additionalProperties": {
        "$ref": "#/definitions/assetMapping"
      }
    }
  },
  "definitions": {
    "assetMapping": {
      "type": "object",
      "required": ["asset"],
      "additionalProperties": false,
      "properties": {
        "asset": {
          "type": "string",
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))(?!.*\\\\).+$",
          "minLength": 1,
          "description": "Safe POSIX path to the USD asset, relative to and contained by the asset root."
        },
        "articulationAsset": {
          "type": "string",
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))(?!.*\\\\).+$",
          "minLength": 1,
          "description": "Optional articulation-enabled USD root, relative to the asset root."
        },
        "scaleMode": {
          "type": "string",
          "enum": ["uniform", "fit"],
          "default": "uniform",
          "description": "How the referenced asset is scaled. 'uniform' applies uniformScale (for pre-sized assets). 'fit' scales a 1 m unit asset to the element's modelled length/width/height."
        },
        "uniformScale": {
          "type": "number",
          "default": 1.0,
          "description": "Uniform scale factor applied to the referenced asset."
        },
        "rotateXDeg": {
          "type": "number",
          "default": 0.0,
          "description": "Rotation about the X axis, in degrees, applied to the referenced asset."
        },
        "zOffsetM": {
          "type": "number",
          "default": 0.0,
          "description": "Additional Z offset, in meters, applied to the referenced asset."
        },
        "assetVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Version of the normalized asset package."
        },
        "sourceUri": {
          "type": "string",
          "format": "uri",
          "description": "Upstream provenance URL."
        },
        "sourceRevision": {
          "type": "string",
          "minLength": 1,
          "description": "Immutable upstream tag or revision."
        },
        "license": {
          "type": "string",
          "minLength": 1,
          "description": "SPDX license identifier for redistribution."
        },
        "metersPerUnit": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Authored asset metres-per-unit metadata."
        },
        "upAxis": {
          "type": "string",
          "enum": ["Y", "Z"],
          "description": "Authored asset up axis."
        },
        "forwardAxis": {
          "type": "string",
          "enum": ["+X", "-X", "+Y", "-Y"],
          "description": "Authored asset forward axis after normalization."
        },
        "bodyMode": {
          "type": "string",
          "enum": ["static", "kinematic", "dynamic"],
          "description": "Default physics body mode when the asset is instantiated."
        },
        "massKg": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Reviewed rigid-body mass in kilograms."
        },
        "wheelRadiusM": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Differential-drive wheel radius in metres."
        },
        "wheelSeparationM": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Distance between differential-drive wheel centers in metres."
        },
        "leftWheelJoint": {
          "type": "string",
          "minLength": 1,
          "description": "Prim path below the asset root for the left wheel joint."
        },
        "rightWheelJoint": {
          "type": "string",
          "minLength": 1,
          "description": "Prim path below the asset root for the right wheel joint."
        }
      }
    }
  }
}
